interface:
  display_name: "Track Execute"
  short_description: "Execute track TODOs with sync and JJ workspaces"
  default_prompt: "Use $track-task-execute: track status --json, jj-task start, cd workspace, $jj for commits, track todo done."
